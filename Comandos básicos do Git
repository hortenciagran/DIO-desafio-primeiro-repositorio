# Comandos básicos do Git 👩🏼‍💻
### Com base no que aprendi no curso de Introdução ao Git/GitHub, as mais importantes na minha opinião e em minhas palavras.


* Tecla TAB = útil para autocompletar endereços e comandos longos
* Teclas ctrl + l = limpa as linhas do seu Bash 
* cd = redireciona para o endereço indicado
* ls = lê o que está em tela, basicamente o que está presente no endereço do qual você acessou
* git init = cria um novo repositório
* git add = adiciona um arquivo e suas modificações para o próximo commit \(stage\), se usar a flag -u adiciona apenas os modificados e deletados, se usar flag . adiciona todos
* git clone = clona um repositório existente para um novo local, porém continua linkado ao repositório original \(para desvincular utilizar git remote rm origin\)
* git commit = salva alterações, e é interessante adicionar a flag \-m com aspas contendo o texto para escrever uma mensagem explicando quais alterações foram aplicadas etc
* git push = é utilizado no processo de sincronização do Git, nesse caso enviando alterações do repositório local para o repositório remoto
* git pull = também utilizado no processo de sincronização do Git, diferente do push, atualiza seu repositório local com alterações do repositório remoto

